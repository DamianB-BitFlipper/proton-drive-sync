import { DriveClient } from "@protontech/drive-sdk";\n\nexport async function createDriveClientFromToken(token: string) {\n  const client = new DriveClient({ token });\n  return client;\n}\n\nexport async function putEncryptedObject(client: any, repoRoot: string, uid: string, payloadJSON: string) {\n  const path = \\\"${repoRoot}/objects/${uid}.json\\\";\n  const blob = new Blob([payloadJSON], { type: "application/json" });\n  return client.put(path, blob);\n}\n\nexport async function listObjects(client: any, repoRoot: string) {\n  const path = \\\"${repoRoot}/objects/\\\";\n  return client.list(path);\n}\n\nexport async function getObject(client: any, path: string) {\n  return client.get(path);\n}